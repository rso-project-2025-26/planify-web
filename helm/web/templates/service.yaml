apiVersion: v1
kind: Service
metadata:
  name: planify-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: planify-frontend
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}